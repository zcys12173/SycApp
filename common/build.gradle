plugins {
    id 'library-plugin'
}

android {
    namespace 'com.syc.mvvm.common'

}

dependencies {
    api ( project(':net'))
    api ( project(':permission'))
    api ( project(':accessibility'))
}